{
    "configurations": [

    {
        "args": [
            "--extensionDevelopmentPath=${workspaceFolder}"
        ],
        "name": "Launch Extension",
        "outFiles": [
            "${workspaceFolder}/out/**/*.js"
        ],
        "preLaunchTask": "npm",
        "request": "launch",
        "type": "pwa-extensionHost"
    },
    {
        "name": "Unity Editor",
        "type": "unity",
        "request": "launch",
        "processId": "${workspaceFolder}/Library/EditorInstance.json"
    }
    ]
}
